/bin/
/dist/
/.claude/
*.test
*.out
.env
.env.*
/.worktrees/

# Dev-lab / local run artifacts generated by running the gateway with the
# example config (repo-relative host_key / evidence / recording paths) or the
# loadtest tool. Never commit these — hostkey.pem is a PRIVATE key.
*.pem
evidence.jsonl
loadtest-evidence.jsonl
/recordings/
/scripts/loadtest/*.pem
/scripts/loadtest/*.jsonl
